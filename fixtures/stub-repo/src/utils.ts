/**
 * Stub repo — utility module.
 *
 * A second source file so the fixture repo has multiple files for scope detection.
 */

export function greet(name: string): string {
  return `Hello, ${name}!`;
}
